- defaults:
    link:
      create: true
      force: true
      relink: true
    shell:
      stdout: true
      stderr: true
      stdin: true

- clean: ['~']

- link:
    ~/.profile: profile
    ~/.bash_profile: bash_profile
    ~/.bashrc: bashrc
    ~/.bash_logout: bash_logout
    ~/.gitconfig: gitconfig
    ~/.Xresources: Xresources
    ~/.xinitrc: xinitrc
    ~/.gtkrc-2.0: gtkrc-2.0
    ~/.ssh: ssh
    ~/.config/Code: config/Code
    ~/.config/bspwm: config/bspwm
    ~/.config/sxhkd: config/sxhkd
    ~/.config/gtk-2.0: config/gtk-2.0
    ~/.config/lemonbar: config/lemonbar

- shell:
#  - [chmod +x installer/*.sh]
  - [chmod 600 ssh/id_rsa]
#  - [pacaur -Sya --needed --noconfirm --noedit --silent --noprogressbar tdrop-git, Installing tdrop]
#  - [pacaur -Sya --needed --noconfirm --noedit --silent --noprogressbar lemonbar-xft-git, Installing lemonbar]
#  - [pacaur -Sya --needed --noconfirm --noedit --silent --noprogressbar ruby-barr, Installing barr]
#  - [pacaur -Sya --needed --noconfirm --noedit --silent --noprogressbar google-chrome, Installing chrome]
#  - [. installer/install-lemonbar.sh, Installing lemonbar]
#  - [. installer/install-barr.sh, Installing barr]
#  - [. installer/install-chrome.sh, Installing chrome]
